# راهنمای جامع انتقال و راه‌اندازی پنل روی سرور اوبونتو ۲۲.۰۴ (Ubuntu Server 22.04)

این راهنما مراحل تنظیم کامل شبکه، انتقال فایل‌های پروژه، نصب ابزارها و در نهایت اجرای برنامه با داکرکامپوز را پوشش می‌دهد.

---

## ۱. تنظیم آدرس IP کامپیوتر/سرور در اوبونتو ۲۲.۰۴ (با Netplan)

در اوبونتو ۲۲.۰۴، تنظیمات شبکه از طریق سرویس **Netplan** مدیریت می‌شود. برای تنظیم کارت شبکه با مشخصات شما:

1. ابتدا نام کارت شبکه فعال خود را با دستور زیر پیدا کنید:
   ```bash
   ip -br link
   ```
   *(به عنوان مثال نام کارت شبکه شما ممکن است `eth0` یا `ens18` یا `enp3s0` باشد. در این بخش ما فرض می‌کنیم نام آن `eth0` است)*

2. فایل کانفیگ شبکه را باز کنید (نام فایل ممکن است متفاوت باشد مثلاً `01-netcfg.yaml` یا `50-cloud-init.yaml`):
   ```bash
   sudo nano /etc/netplan/01-netcfg.yaml
   ```

3. محتوای فایل را مطابق ساختار زیر ویرایش کنید (آدرس IP و گیت‌وی شما هماهنگ با درخواستتان تنظیم شده است):
   ```yaml
   network:
     version: 2
     renderer: networkd
     ethernets:
       eth0:  # نام کارت شبکه خود را اینجا جایگذاری کنید
         dhcp4: no
         addresses:
           - 100.100.100.10/28
         routes:
           - to: default
             via: 100.100.100.1
         nameservers:
           addresses:
             - 8.8.8.8
             - 1.1.1.1
   ```

4. برای اعمال کانفیگ جدید دستورات زیر را اجرا کنید:
   ```bash
   sudo netplan generate
   sudo netplan apply
   ```

5. بررسی صحت اعمال شبکه:
   ```bash
   ip addr show eth0
   ping -c 3 100.100.100.1
   ```

---

## ۲. دریافت و انتقال فایلهای پروژه به سرور

برای انتقال فایل‌های پروژه از این محیط کاربری به سرور خود دو راه استاندارد دارید:

### روش اول: دانلود مستقیم نسخه ZIP (آسان‌ترین راه)
1. از منوی **تنظیمات بالا/راست (Settings)** در محیط کاربری AI Studio گزینه **Export as ZIP** را انتخاب یا فایل‌های پروژه را دانلود کنید.
2. برای انتقال فایل ZIP به سرور اوبونتو، از دستور `scp` در ترمینال کامپیوتر شخصی خودتان استفاده کنید:
   ```bash
   scp path/to/project.zip root@100.100.100.10:/root/
   ```
3. در اوبونتو فایل را استخراج کنید:
   ```bash
   sudo apt-get install unzip -y
   unzip project.zip -d /root/sas-radius
   cd /root/sas-radius
   ```

### روش دوم: استفاده از گیت (Git)
همچنین می‌توانید ابتدا پروژه را به گیت‌هاب اگزپورت کنید و سپس روی سرور خود کلون یا گیت بکنید:
```bash
git clone <آدرس_مخزن_شما> /root/sas-radius
cd /root/sas-radius
```

---

## ۳. اجرای خودکار اسکریپت نصب و راه‌اندازی (Setup)

ما یک اسکریپت آماده به نام `setup-ubuntu.sh` را در ریشه اصلی پروژه قرار داده‌ایم که کارهای زیر را به صورت خودکار انجام می‌دهد:
- آپدیت پکیج‌های سیستم
- نصب داکر (Docker) و داکر کامپوز (Docker Compose)
- کامپایل و بالا آوردن کانتینرهای پروژه

برای اجرای آن کافیست دستورات زیر را در ریشه پروژه (درون سرور اوبونتو) وارد کنید:

1. مجوز دسترسی اجرایی به اسکریپت بدهید:
   ```bash
   chmod +x setup-ubuntu.sh
   ```

2. اسکریپت را به عنوان کاربر ریشه (root) اجرا کنید:
   ```bash
   sudo ./setup-ubuntu.sh
   ```

---

## ۴. مدیریت سرویس‌ها و کانتینرها (Docker Commands)

پس از اجرای موفقیت‌آمیز سرویس‌ها در پس‌زمینه اجرا خواهند شد. از دستورات زیر برای مدیریت آن‌ها استفاده کنید:

* **مشاهده وضعیت کانتینرها:**
  ```bash
  docker compose ps
  ```

* **مشاهده لاگ‌های کل سیستم (بخش فرانت، دیتابیس و رادیوس):**
  ```bash
  docker compose logs -f
  ```

* **مشاهده لاگ‌های بخش رادیوس (FreeRADIUS):**
  ```bash
  docker compose logs -f sas-radius-core
  ```

* **غیرفعال کردن و متوقف کردن موقت سرویس‌ها:**
  ```bash
  docker compose down
  ```

* **راه‌اندازی مجدد سرویس‌ها:**
  ```bash
  docker compose up -d
  ```

---

## ۵. دسترسی به پنل کاربری

پس از به پایان رسیدن عملیات داکر، وب‌سرور پنل روی پورت پیش‌فرض HTTP (پورت **80**) بالا خواهد آمد. شما می‌توانید از طریق مرورگر کامپیوترهایی که در رنج شبکه شما قرار دارند، با پورت ۸۰ یا به طور مستقیم آدرس زیر وارد پنل شوید:

```text
http://100.100.100.10/
```

همچنین پورت‌های **1812** و **1813** به صورت UDP به روی سرور باز شده‌اند تا روترهای میکروتیک یا تجهیزات شبکه شما بتوانند درخواست‌های احراز هویت (Authentication) و اکانتینگ (Accounting) را ارسال نمایند.

* اطلاعات پیش‌فرض اتصال دیتابیس (MariaDB) و درگاه‌ها در داخل فایل `docker-compose.yml` ذخیره شده است که در صورت نیاز به تغییر رمز عبور پایگاه داده می‌توانید آن را ویرایش کنید.
